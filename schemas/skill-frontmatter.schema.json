{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cairn/schemas/skill-frontmatter.schema.json",
  "title": "Skill Frontmatter",
  "description": "Schema for SKILL.md YAML frontmatter in Cairn plugins (Agent Skills spec fields only, https://agentskills.io/specification)",
  "type": "object",
  "required": ["name", "description"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "maxLength": 64,
      "description": "Kebab-case skill identifier, must match the directory name"
    },
    "description": {
      "type": "string",
      "minLength": 20,
      "maxLength": 1024,
      "description": "Trigger description explaining what the skill does and when to invoke it"
    },
    "license": {
      "type": "string",
      "description": "License name or bundled license file"
    },
    "compatibility": {
      "type": "string",
      "maxLength": 500,
      "description": "Environment requirements"
    },
    "metadata": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Client-specific string key-value pairs"
    },
    "allowed-tools": {
      "type": "string",
      "description": "Space-delimited list of pre-approved tools"
    }
  },
  "additionalProperties": false
}
